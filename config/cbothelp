* project [<regexp>] - print the project list.
* site [<regexp>] - print the site list.
* summary <project name> - show project summary information.
* schedule <project name> [<build parameters>] - schedule a new build.
  Parameters:
    -type exp|nighty|cont - build type.
    -repo <url> - alternative repository url.
    -branch <name> - name of the repository branch/module.
    -tag <name> - name of the repository tag.
    -conf dbg|rel|reldbg|minsize - configuration type for compilation.
    -compiler <name>[-<version>] - compiler name and optionally version.
    -library <name>[-<version>] - library name and optionally version.
    -site <name> - specify that the build should run on this site.
    -os <name> [<osbits>] - name of the OS to run the build.
* status <schedule id> - print schedule status.
* help - print this help message.
* version - print cdash and bot versions.
