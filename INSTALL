1. mkdir -p ~/.config/cdashbot/ && cp config/* ~/.config/cdashbot/

2. configure  ~/.config/cdashbot/cbotrc

3. make deps

4. make compile

5. make console or make start (need create /var/log/cdashbot before) 
 
